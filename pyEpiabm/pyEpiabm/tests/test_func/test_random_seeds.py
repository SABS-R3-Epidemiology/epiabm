import unittest


class TestPopulationRandomSeeds(unittest.TestCase):
    """Placeholder test
    """

    def test_construct(self):
        self.assertEqual(1, (3-1))
        # add random seed tests here
        # also add whole simulation flow tests etc to separate file
        # and some workflows to a integration test file
        # but ensure 100% unit coverage, so if removing tests changes that,
        # and you can't fix it, don't do that.


if __name__ == '__main__':
    unittest.main()
